run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - gosec

issues:
  exclude-use-default: true
  max-issues-per-linter: 50
  max-same-issues: 50
